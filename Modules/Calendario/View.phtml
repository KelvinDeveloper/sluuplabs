<?php include 'CalendarBase.php'; ?>

<div class="calendar-header">
	
	<i class="material-icons calendarLast">arrow_back</i>

	<select>
		<?php foreach ($Month as $k => $v){ ?>
		<option value="<?=str_pad( $k, 2, 0, STR_PAD_LEFT )?>"><?=$v?></option>
		<?php } ?>
	</select>
	<input value="<?=date('Y')?>" maxlength="4"> 

	<i class="material-icons calendarPrevious">arrow_forward</i>
</div>

<table class="calendar">
	
	<?php

	$Calendar = '';
	$CalendarHeader = false;

	for ($i = 0; $i <= 5; $i++) { 

		if(! $CalendarHeader ){

			$Calendar .= '<tr>';

			for ($i3 = 0; $i3 <= 6 ; $i3++) { 

				$Calendar .= '<th>' . $Week[ $i3 ] . '</th>';
			}
			$Calendar .= '</tr>';
			$CalendarHeader = true;
		}

		$Calendar .= '<tr data-week="' . $i . '">'; 
		$DayWeek = 0;

		for ($i2 = 0; $i2 <= 6 ; $i2++) { 

			$Calendar .= '<td data-day-week="' . $DayWeek . '" class="openModal" title="Registros" data-size="large" data-parent="#ModuleCalendario" for="CreateCalendar">';
			$Calendar .= '<span class="day"></span>';
			$Calendar .= '<span class="events"></span>';

			$Calendar .= '</td>';

			$DayWeek++;
		}

		$Calendar .= '</tr>';
	}

	echo $Calendar;

	?>

</table>

<link href="/Modules/Calendario/Public/css/styles.css" rel="stylesheet">
<script type="text/javascript">
<?php
echo "var CalendarDateNow = '" . date('Y-m-d') . "';";
?>
</script>
<script type="text/javascript" src="/Modules/Calendario/Public/js/scripts.js"></script>